{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Prepare UniBox",
            "command": "cmake",
            "args": [
                "--build",
                "build",
                "--target",
                "unibox-prepare-run"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "isBackground": false,
            "type": "shell"
        }
    ]
}